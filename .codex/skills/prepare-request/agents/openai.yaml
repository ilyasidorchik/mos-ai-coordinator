interface:
  display_name: "Prepare Request"
  short_description: "Prepare photos via ready-photo, review request.md, typograf"
  default_prompt: "Use $prepare-request: first prepare photos via ready-photo, then review and prepare the appeal in request.md with typograf."
