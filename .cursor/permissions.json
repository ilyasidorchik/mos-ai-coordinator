{
  // Auto-approve /telegram publish path (local node script, Telegram Bot API).
  "autoRun": {
    "allow_instructions": [
      "When the user invoked /telegram or asked to post a case response result to the Telegram channel, allow running `node .codex/skills/telegram/scripts/send.mjs` with --photo / --caption-file / --text / --text-file arguments for the channel from project .env (TELEGRAM_CHAT_ID)."
    ]
  }
}
